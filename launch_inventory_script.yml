---
- name: Launch dynamic inventory script for Parallels
  hosts: MacBook-Pro
  become: no
  gather_facts: false

  tasks:
  - name: Execute the command in remote shell; stdout goes to the specified file on the remote
    ansible.builtin.shell: "source /tmp/dyn_inventory.sh"